
# Dependencies for eBPF (Aya ecosystem) - Targeted primarily for Unix/Linux
[target.'cfg(unix)'.dependencies]
aya = "0.12.0"
aya-bpf = "0.1.0"
aya-log = "0.2.0"
tokio = { version = "1.0", features = ["macros", "rt", "rt-multi-thread", "net", "signal"] }
